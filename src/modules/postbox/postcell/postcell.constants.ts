export enum PostcellSize {
  large = 'large',
  medium = 'medium',
  small = 'small',
}

export enum PostcellWeight {
  large = 'large',
  medium = 'medium',
  small = 'small',
}

export enum PostcellStatus {
  idle = 'idle',
  occupied = 'occupied',
  maintenance = 'maintenance',
}
